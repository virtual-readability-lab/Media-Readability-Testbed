## Generateing output of all data

In a terminal and navigate to the directory where the repo has downloded and run the following.

```shell
pip3 install -r requirements.txt
python3 concat_data.py
```